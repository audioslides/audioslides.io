<h2>Edit Presentation</h2>

<%= render "_form.html", Map.put(assigns, :action, presentation_path(@conn, :update, @presentation)) %>

<span><%= link "Back", to: presentation_path(@conn, :index) %></span>
